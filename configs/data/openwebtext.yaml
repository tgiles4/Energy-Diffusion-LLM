train: openwebtext
valid: wikitext103
tokenizer_name_or_path: gpt2
cache_dir: ${path}/data
wrap: True
streaming: False
# HF datasets.map tuning (tokenize + group). null = use all CPUs Slurm assigned.
preprocess_num_proc: null
preprocess_map_batch_size: 8000
# Arrow writer batch; null = max(1000, preprocess_map_batch_size)
preprocess_writer_batch_size: null
